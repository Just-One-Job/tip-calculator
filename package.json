{
  "name": "just-one-job-microapps",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:tip": "cd apps/tip-calculator && npx expo start",
    "web:tip": "cd apps/tip-calculator && npx expo start --web",
    "build:web:tip": "cd apps/tip-calculator && npm run build:web"
  }
}
